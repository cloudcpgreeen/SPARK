# 交付约定（DEPLOYMENT）

> 约定三 · 什么时候算"做好"、怎么发布。

## 1. 交付门禁（提交 / 打 tag 前必须满足）

| 门禁 | 检查 |
| --- | --- |
| 测试 | `cargo test` 全绿 |
| 构建 | `cargo build` 干净（无 warning 视为干净） |
| 契约 | 实现满足 `wit/` 契约；契约变更已更新版本号（CONTRACT.md §4） |

任一不满足 → 不提交，先修。

## 2. 版本与发布

- 稳定版用 annotated tag：`git tag -a vX.Y.Z -m "..."`。
- 版本号与 WIT package 主版本 + 功能状态对齐；破坏性变更升主版本。
- tag 后同步推送远端（`git push origin <tag>`）。

## 3. 运行与配置（预留）

**当前 SPARK 尚无具体运行形态**（可能是 CLI / Spin 组件 / 纯库）。约定框架如下，idea 落地后按实际形态填充：

- 配置项集中声明（环境变量或配置文件），不散落硬编码。
- 生产/开发/离线等运行模式如果存在，明确区分并写清。

## 4. 数据与安全（预留）

- 持久化形态、密钥管理、日志脱敏等约定在 idea 落地后明确。
- 约定：**密钥不进代码仓库**；日志不落敏感值。

## 5. 变更记录

- 每个发布的 tag 在 README 或 CHANGELOG 里说明包含什么。
