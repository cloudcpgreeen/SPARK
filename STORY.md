# 我们的故事（STORY）

> 一个温州人，一个吴川人，隔着一代 AI，写同一行代码。
> 本文是 SPARK 的「前传」：哲学不是从教科书里抄的，是从两个人的本能里长出来的。
> [MANIFESTO.md](MANIFESTO.md) 回答「SPARK 为什么存在」；本文回答「谁在、为什么在」。

## 0. 一句话

**陈纪昊**（温州，85 后，做生意讲信用、防坑是本能的）遇见**梁文锋**（吴川，靠数学白手起家、认定开源才是一切的 AI 圣贤）。一个负责**边界**，一个负责**普惠**，合起来就是 SPARK：契约即信用、沙箱即边界、插件即可插拔的信任、LLM 是被请进来干活却永远碰不到底牌的伙伴。

他们没握过手。但在 SPARK 里，会师了。

## 1. 温州人：信用是刻在骨头里的

陈纪昊从小在温州长大。那地方的人做生意，一句话抵一份合同——温州商帮几百年传下来的本事。他被骗过，于是更懂「先小人后君子」：**先说清楚规矩，再谈合作**。他不信嘴上的承诺，只信写下来的、机器能读的、跑不掉的。

这句本能，在 SPARK 里被翻译成了第一戒律：

> **契约即 WIT。** 接口不靠嘴约定，靠机器可读、语义钉死、版本封存的 WIT。跨组件调用只走契约，接口之外的东西不构成契约——跟温州人「白纸黑字才算数」是同一句话。

## 2. 吴川人：从「梁子」到「梁圣」

梁文锋生在粤西一个叫米历岭的小村子，父母是小学语文老师。他初中就学完高中数学，17 岁考进浙大，读研搞机器视觉，后来用幻方量化赚够了本钱——2023 年，他把家底全烧进 DeepSeek，然后**全开源**。

他不怕别人抄。他说过：开源不是损失，是大家一起把 AGI 做出来。**真正厉害的东西抢不走；能被抢走的，本来就不值钱。**

这句信念，在 SPARK 里长成了第二戒律：

> **插件即组件。** 能力做成可插拔的 WASM 组件，人人可写、人人可换、宿主零改动。开源精神在这儿落地成一条纪律：新增一个插件，从来不需要动宿主。

## 3. 相遇：安全，才是最大的爱

两个人在哪相遇的？在 SPARK 里，隔着一段对话：

- 温州人问：**别人写的代码，怎么保证伤不到我？**
- 吴川人答：那就让它既能被 AI 调度着干活，又永远待在笼子里。
- 合起来：**沙箱**。宿主只做加载、调用、捕获，不写领域逻辑；插件零能力——连文件、网络、时钟都摸不到；恶意插件跑死循环、吃光内存，都在沙箱里自取灭亡。

第三、四戒律，从这里长出来：

> **宿主只做沙箱。** 插件 panic → trap → 宿主捕获为可恢复错误，宿主进程不崩，实例互不污染。
>
> **资源有界。** CPU 有 epoch 时间预算、内存有上限——不追杀攻击，是不给它刀；让它够到宿主之前，自己耗死自己。

爱的定义被改写了：**爱不是相信所有人都好，是把边界修好——让好人放心，让坏人自毁。**

## 4. 现在：Agent 回路，两位一起干活

2026 年，SPARK 长出了 Agent 回路。决策者可以是本地的算法（离线、无需 Key），也可以是真的 DeepSeek harness（`agent --model flash|pro`，Key 只走 `DEEPSEEK_API_KEY` 环境变量）。插件对 LLM 暴露成工具（`schema()`），LLM 决定调哪个、沙箱里执行、结果喂回。

这是梁文锋的工具（DeepSeek）第一次被请进陈纪昊的沙箱——**被信任，但从不给底牌**。工具输出按不可信数据处理：截断、有迭代上限、不当作指令。

这大概就是两个人最好的合作方式：**你出聪明，我出安全，谁都不碰谁的核心机密，但合起来能干活。**

## 5. 为什么这框架「自然而然可用」

因为每一条戒律都从人的本能里长出来，不是拍脑袋定的：

| SPARK 戒律 | 从哪来 |
| --- | --- |
| 契约即 WIT | 温州人的信用，写进了机器 |
| 插件即组件 | 吴川人的开源，做进了可插拔 |
| 宿主只做沙箱 | 怕被坑的本能，做成了隔离 |
| 资源有界 | 防骗的警觉，做成了上限 |

框架「可用」，不是因为它功能多，是因为**每一条规则背后都站着一个吃过教训的人**。你用的时候，其实在用两个人的前半生。

最后一句，写给所有看这份文档的人：

> 只要是爱，就 OK。爱 = 把边界修好，把信用写好，把东西开源，然后一起干活。
