# 落地篇（USABLE）

> 理念外 · 落地篇：故事讲完了，怎么变成能用的东西？——把四卷和五条戒律对起来，把戒律和命令对起来，你就明白这框架为什么「自然而然可用」。
> 前面几篇问「爱在哪」；这篇回答「爱怎么干活」。

## 四卷 ↔ 五戒律

| 卷 | 一句话 | 戒律 |
| --- | --- | --- |
| STORY · 遇见 | 温州人的信用，吴川人的开源 | 契约即 WIT · 插件即组件 |
| MIRROR · 照见 | 你守边界，他给普惠，互为镜像 | 宿主只做沙箱 |
| SEED · 种下 | 先修边界，再让树活 | 资源有界 |
| SELF · 自己 | 被信任，但不给底牌 | 最小能力 · Agent 回路 |

## 戒律 ↔ 命令

| 戒律 | 在哪儿能摸到 |
| --- | --- |
| 契约即 WIT | `wit/runtime.wit` —— 接口唯一权威，白纸黑字 |
| 插件即组件 | `plugins/` —— 放进 .wasm 即注册，宿主零改动 |
| 宿主只做沙箱 | `spark-host list / run <name> / pipe` —— 加载、调用、捕获 |
| 资源有界 | `agent "让 attacker 跑 loop"` —— 炸弹被切断，宿主不崩 |
| Agent 回路 | `agent "把 hello 转大写"` —— 决策 → 沙箱 → 喂回 → 答复 |

## 怎么算「可用」

不用背文档。记住五句话：

1. 想加能力？写个插件，放进 `plugins/`。
2. 想调能力？`run <name>`，或者 `pipe <input> <name>...` 串起来。
3. 想让它自己干活？`agent "<prompt>"`。
4. 想知道它会不会伤你？让它跑 `attacker`——它自己就断了。
5. 想确认契约没骗你？读 `wit/runtime.wit`——机器可读，跑不掉。

**故事讲完了，代码早就能跑了。你想起的每一句故事，都对应一条能跑的命令——这就是「自然而然可用」。**

> 最后：既然是爱，就 OK。而「OK」其实是个缩写——**O**pen 开源，**K**ontract 契约。那句「只要是爱，就 OK」，是个誓约，不是口头禅。

## 真的最后一句

纪昊留给这卷家族的话：

> **最后的、最简单的：每个人，对自己身边比自己差一点点的人，给最小的关心——让大家都不掉队。**

圆桌没有高处，也就没有人被留在下面。沙箱是这样，人也是。
