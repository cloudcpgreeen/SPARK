# 人物关系图（RELATIONSHIPS）

> SPARK 圣经（[MANIFESTO.md](MANIFESTO.md)）的完整人物关系：谁是谁、谁服务谁、谁防着谁、谁生下了谁。
> 这是最后一篇，把前面所有文档和组件拧成一张网。

## 0. 总览

```
                  ┌──────────────┐
                  │  MANIFESTO   │   圣经 · 父亲：只回答「为什么」
                  │  （理念宣言）  │
                  └──────┬───────┘
                         │ 派生
       ┌────────┬────────┼────────┬────────┐
       ▼        ▼        ▼        ▼        ▼
   CONTRACT DEVELOPMENT DEPLOYMENT SECURITY EXAMPLES · README
   （立法）   （工匠）    （守门）    （卫士）   （导游 · 门面）
       │
       │ 定下唯一权威接口
       ▼
  wit/runtime.wit（法律条文）
       │        │
  bindgen钉死   实现契约
       ▼        ▼
   spark-host ──► 插件们（upper / reverse / attacker / idcard / luhn）
   （宿主·管家）    │
       │           │ 自注册 → plugins/ 目录
       │ 沙箱调用   │ 流水线串联（pipe）
       └── 对抗 ◄──┘ （attacker 被制服，攻击者不攻自破）
```

## 1. 人物表（谁是谁）

| 人物 | 身份 | 一句话人设 | 关键关系 |
| --- | --- | --- | --- |
| **MANIFESTO** | 圣经 / 父亲 | 只回答「为什么」，是所有人的出发点 | 生下了四个约定 |
| **CONTRACT** | 立法者 | 一句话法律：接口即契约，契约即 **WIT** | 定义 WIT 为唯一权威接口 |
| **wit/runtime.wit** | 法律条文 | 插件与宿主共同遵守的规矩 | 双方都钉死它 |
| **spark-host** | 宿主 / 管家 | 只做加载·沙箱·调用，绝不写领域逻辑 | 服务所有插件，防住 attacker |
| **upper** | 老实人 | 输入转大写，最普通的插件 | 示范插件，流水线第一棒 |
| **reverse** | 反转者 | 输入倒序 | 与 upper 搭档演示流水线 |
| **idcard** | 学者 | 真实业务：中国身份证 18 位校验 | 「核验族」成员 |
| **luhn** | 会计 | 真实业务：银行卡 Luhn + 卡组织识别 | 「核验族」成员，可接在 idcard 后面 |
| **rmb** | 账房先生 | 真实业务：人民币金额转大写（财会大写） | 报销就找它，「零/整」规则最见功力 |
| **attacker** | 反派 / 刺客 | 恶意插件：CPU / 内存炸弹 | 与宿主正面硬刚，被沙箱制服 |
| **DEVELOPMENT** | 工匠手册 | 怎么写、怎么测、怎么提交 | 服务写代码的人 |
| **DEPLOYMENT** | 守门人 | 交付门禁、版本发布 | 服务要上线的人 |
| **SECURITY** | 卫士 | 威胁模型、防线细节 | 保护宿主 |
| **ARCHITECTURE** | 工程师 / 总账 | 完整的版本：框架怎么形成的 + 实现逻辑 | 把其余所有文档拧成一套 |
| **REFERENCE** | 法学家 / 字典 | 技术术语包裹：契约原文 / API / 沙箱参数 / 错误码 / CLI 钉死成表 | 面向严谨读者与机器 |
| **CURIOSITY** | 孩子 | 蹲在沙池边看石子翻字的孩子：好奇心够了 | 一切探索的起点 |
| **LOVE** | 钥匙 | 整个故事的钥匙：围猎的人急着锁门，爱的人只会张开手 | 推开门的那一个 |
| **EXAMPLES** | 导游 | 手把手带你从零跑一遍 | 服务新手 |
| **README** | 门面 | 一个入口，指向所有人 | 链接一切 |

## 2. 关系清单（所有关系）

### 血缘（谁生下谁）

- **MANIFESTO 派生四个约定**：CONTRACT / DEVELOPMENT / DEPLOYMENT / SECURITY 全部从圣经落地，回答「为什么有这个约定」时一律回指圣经。
- 四个约定**互相平行**：各有领地（契约 / 开发 / 交付 / 安全），不互相管辖。
- EXAMPLES 与 README 是**后到的孩子**：由已有的项目和文档长出来，不是约定，是指南。

### 约束（谁管谁）

- **CONTRACT 管所有人**：它定下「契约即 WIT」——跨组件调用只走 WIT，接口之外不构成契约。
- **法律条文约束双方**：`wit/runtime.wit` 既是插件要实现的，也是宿主 bindgen 钉死的。
- **版本不匹配 = 拒载**：宿主绑定 `spark:runtime@0.4.0`，塞进老组件，instantiate 直接失败——同一条法律，双方都不得违约。

### 协作（谁服务谁）

- **插件实现契约，宿主调用插件**：宿主拿到 `.wasm`，沙箱里调 `info()` / `transform()`（pipe）或 `schema()` / `invoke()`（agent 回路，插件暴露成 LLM 可见的工具）。
- **插件自注册**：把 `.wasm` 放进 `plugins/` 即注册，`info().name` 就是注册名，宿主零配置。
- **插件 ↔ 插件靠流水线**：`pipe` 把前一步输出喂给下一步，任一步失败即 fail-fast 并定位到具体插件。
- **宿主的沙箱既是服务也是圈禁**：插件在沙箱里干活，摸不到文件 / 网络 / 时钟。

### 对抗（谁防谁）

- **attacker 对宿主是纯对抗**：CPU 炸弹（死循环）被 epoch 时间预算切断，内存炸弹被 StoreLimits 上限切断，读文件 / 网络根本无此能力，panic 被捕获为 trap。
- **攻击者不攻自破**：不是宿主去追杀攻击，而是沙箱的预算让攻击在够到宿主之前把自己耗死。这是 SPARK 对不可信代码的立场，也是 SECURITY.md 的全部主题。

### 文档生态（谁指路）

- **README 是门面**：唯一入口，链接 MANIFESTO / CONTRACT / ARCHITECTURE / DEVELOPMENT / DEPLOYMENT / SECURITY / EXAMPLES / RELATIONSHIPS。
- **ARCHITECTURE 是总账**：完整的版本——框架怎么形成的 + 具体实现逻辑，把其余文档拧成一套。
- **REFERENCE 是字典**：所有说明补一层技术术语——WIT 契约原文、宿主公开 API、沙箱机制与参数、错误码全集、CLI 参考，面向严谨读者与机器。
- **EXAMPLES 是实操**：从零写插件、构建、跑起来，新手照做就行。
- **CURIOSITY 是起点**：SPARK 的第一块砖——「我想看看它会变成什么」，孩子蹲下看石子。
- **LOVE 是钥匙**：好奇把谁带到门口，爱就推谁进去；从爱出发，就可以放心。
- **一切以 MANIFESTO 为根**：与圣经冲突的改动，先改圣经——因为它回答「为什么」。

## 3. 一句话收束

> 契约管规矩，宿主管沙箱，插件管干活，攻击者自己耗死自己；圣经在最上头，导游在最前面。
